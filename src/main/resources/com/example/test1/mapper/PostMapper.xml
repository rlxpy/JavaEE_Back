<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.PostMapper">

    <select id="getAllPosts" resultType="com.example.test1.entity.Post">
        SELECT p.*, u.nickname, u.avatar
        FROM posts p
        LEFT JOIN users u ON p.user_id = u.id
        <where>
            <if test="keyword != null and keyword != ''">
                (p.title LIKE CONCAT('%', #{keyword}, '%') OR p.content LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY p.create_time DESC
    </select>

    <insert id="insertPost" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO posts (user_id, game_id, title, content)
        VALUES (#{userId}, #{gameId}, #{title}, #{content})
    </insert>

    <select id="getPostById" resultType="com.example.test1.entity.Post">
        SELECT p.*, u.nickname, u.avatar
        FROM posts p
                 LEFT JOIN users u ON p.user_id = u.id
        WHERE p.id = #{id}
    </select>

    <update id="incrementViewCount">
        UPDATE posts SET view_count = view_count + 1 WHERE id = #{id}
    </update>

    <update id="incrementLikeCount">
        UPDATE posts SET like_count = like_count + 1 WHERE id = #{id}
    </update>

    <update id="decrementLikeCount">
        UPDATE posts SET like_count = like_count - 1 WHERE id = #{id}
    </update>

    <select id="getPostsByUserId" resultType="com.example.test1.entity.Post">
        SELECT p.*, u.nickname, u.avatar
        FROM posts p
        LEFT JOIN users u ON p.user_id = u.id
        WHERE p.user_id = #{userId}
        <if test="keyword != null and keyword != ''">
            AND (p.title LIKE CONCAT('%', #{keyword}, '%') OR p.content LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY p.create_time DESC
    </select>

    <delete id="deletePost">
        DELETE FROM posts WHERE id = #{id} AND user_id = #{userId}
    </delete>

    <delete id="deletePostByAdmin">
        DELETE FROM posts WHERE id = #{id}
    </delete>
</mapper>