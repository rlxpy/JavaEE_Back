<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.GameMapper">

    <update id="updateGameRating">
        UPDATE games SET average_rating = #{avgRating} WHERE id = #{gameId}
    </update>

    <select id="getAverageRatingByGameId" resultType="Double">
        SELECT AVG(rating) FROM comment WHERE game_id = #{gameId} AND (parent_id IS NULL OR parent_id = 0)
    </select>

</mapper>