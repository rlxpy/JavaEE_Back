<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.UserMapper">

    <select id="getUserById" resultType="com.example.test1.entity.User">
        SELECT id, username, password, nickname, avatar, role
        FROM users
        WHERE id = #{id}
    </select>

    <select id="getAllUsers" resultType="com.example.test1.entity.User">
        SELECT id, username, password, nickname, avatar, role FROM users
    </select>

    <select id="getUserByUsername" resultType="com.example.test1.entity.User">
        SELECT id, username, password, nickname, avatar, role
        FROM users
        WHERE username = #{username}
    </select>

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, nickname, avatar, role)
        VALUES (#{username}, #{password}, #{nickname}, #{avatar},#{role})
    </insert>

    <update id="updateUserById">
        UPDATE users
        <set>
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="nickname != null and nickname != ''">nickname = #{nickname},</if>
            <if test="avatar != null and avatar != ''">avatar = #{avatar},</if>
            <if test="role != null">role = #{role},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteUserById">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <delete id="deleteAllUsers">
        DELETE FROM users
    </delete>

    <select id="getUsersByCondition" resultType="com.example.test1.entity.User">
        SELECT id, username, password, nickname, avatar, role
        FROM users
        <where>
            <if test="keyword != null and keyword != ''">
                AND (username LIKE CONCAT('%', #{keyword}, '%') OR nickname LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="role != null">
                AND role = #{role}
            </if>
        </where>
        ORDER BY id DESC
    </select>

</mapper>