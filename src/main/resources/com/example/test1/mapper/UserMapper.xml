<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.UserMapper">

    <select id="getUserById" resultType="com.example.test1.entity.User">
        SELECT id, username, password, nickname, avatar FROM users WHERE id = #{id}
    </select>

    <select id="getAllUsers" resultType="com.example.test1.entity.User">
        SELECT id, username, password, nickname, avatar FROM users
    </select>

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, nickname, avatar) VALUES (#{username}, #{password}, #{nickname}, #{avatar})
    </insert>

    <update id="updateUserById">
        UPDATE users SET username=#{username}, password=#{password}, nickname=#{nickname}, avatar= #{avatar} WHERE id = #{id}
    </update>

    <delete id="deleteUserById">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <delete id="deleteAllUsers">
        DELETE FROM users
    </delete>

</mapper>